import type { Job } from "./data";
import type { User } from "./store";

export type Currency = "INR" | "USD";
export type MarketBand = { grade: string; experienceMin: number; experienceMax: number; geography: string; region: string; country: string; currency: Currency; p25: number; p50: number; p75: number; p90: number; roleMin: number; roleMax: number };
export const GRADES = ["IC3", "IC4", "IC5 / Senior", "M1", "M2", "Director", "VP"];
export const GEOGRAPHIES = ["APAC", "EMEA", "Americas", "MEA"];
export const REGIONS = ["South Asia", "Southeast Asia", "ANZ", "Western Europe", "North America", "GCC"];
export const COUNTRIES = ["India", "Singapore", "UAE", "United Kingdom", "United States", "Germany"];
export const JOB_MARKET: Record<string, MarketBand> = { j1: { grade: "IC5 / Senior", experienceMin: 6, experienceMax: 10, geography: "APAC", region: "South Asia", country: "India", currency: "INR", p25: 38, p50: 52, p75: 65, p90: 78, roleMin: 45, roleMax: 65 }, j2: { grade: "M1", experienceMin: 8, experienceMax: 12, geography: "APAC", region: "South Asia", country: "India", currency: "INR", p25: 48, p50: 60, p75: 72, p90: 88, roleMin: 50, roleMax: 72 }, j3: { grade: "IC5 / Senior", experienceMin: 5, experienceMax: 9, geography: "APAC", region: "South Asia", country: "India", currency: "INR", p25: 32, p50: 44, p75: 55, p90: 64, roleMin: 38, roleMax: 55 }, j4: { grade: "M1", experienceMin: 9, experienceMax: 14, geography: "APAC", region: "Southeast Asia", country: "Singapore", currency: "USD", p25: 160, p50: 195, p75: 230, p90: 260, roleMin: 180, roleMax: 230 }, j5: { grade: "IC4", experienceMin: 3, experienceMax: 6, geography: "APAC", region: "South Asia", country: "India", currency: "INR", p25: 18, p50: 26, p75: 35, p90: 42, roleMin: 22, roleMax: 35 }, j6: { grade: "IC5 / Senior", experienceMin: 5, experienceMax: 9, geography: "APAC", region: "South Asia", country: "India", currency: "INR", p25: 24, p50: 34, p75: 42, p90: 50, roleMin: 28, roleMax: 42 }, j7: { grade: "IC4", experienceMin: 4, experienceMax: 8, geography: "APAC", region: "South Asia", country: "India", currency: "INR", p25: 20, p50: 30, p75: 40, p90: 48, roleMin: 24, roleMax: 40 }, j8: { grade: "M1", experienceMin: 7, experienceMax: 12, geography: "APAC", region: "South Asia", country: "India", currency: "INR", p25: 28, p50: 38, p75: 48, p90: 58, roleMin: 30, roleMax: 48 }, j9: { grade: "M1", experienceMin: 7, experienceMax: 11, geography: "APAC", region: "South Asia", country: "India", currency: "INR", p25: 28, p50: 40, p75: 50, p90: 60, roleMin: 32, roleMax: 50 }, j10: { grade: "M2", experienceMin: 10, experienceMax: 16, geography: "APAC", region: "South Asia", country: "India", currency: "INR", p25: 62, p50: 78, p75: 95, p90: 115, roleMin: 70, roleMax: 95 }, j11: { grade: "M1", experienceMin: 8, experienceMax: 14, geography: "APAC", region: "South Asia", country: "India", currency: "INR", p25: 40, p50: 55, p75: 70, p90: 85, roleMin: 45, roleMax: 70 }, j12: { grade: "IC4", experienceMin: 3, experienceMax: 7, geography: "APAC", region: "South Asia", country: "India", currency: "INR", p25: 22, p50: 34, p75: 48, p90: 58, roleMin: 28, roleMax: 48 } };
export function formatComp(n: number, currency: Currency) { return currency === "INR" ? `₹${n} LPA` : `$${n}K`; }
export function profileDefaults(user: User | null) { return { gradeLevel: user?.gradeLevel || "IC5 / Senior", experienceYears: user?.experienceYears ?? 8, functionArea: user?.functionArea || user?.title || "Product", geography: user?.geography || "APAC", region: user?.region || "South Asia", country: user?.country || "India", currency: (user?.currency || "INR") as Currency, compMin: user?.compMin ?? 42, compMax: user?.compMax ?? 58, skills: user?.skills ?? [] }; }
export type MapRow = { dimension: string; you: string; role: string; market: string; fit: "strong" | "aligned" | "gap"; negotiate: string };
export function mapCandidateToRole(user: User | null, job: Job): MapRow[] { const p = profileDefaults(user); const m = JOB_MARKET[job.id]; if (!m) return []; const skillHits = p.skills.filter((s) => [...job.tags, job.title, job.function].join(" ").toLowerCase().includes(s.toLowerCase())); const skillMiss = job.tags.filter((t) => !p.skills.some((s) => s.toLowerCase().includes(t.toLowerCase()) || t.toLowerCase().includes(s.toLowerCase()))); const expFit = p.experienceYears >= m.experienceMin && p.experienceYears <= m.experienceMax + 2 ? "strong" : p.experienceYears >= m.experienceMin - 1 ? "aligned" : "gap"; const mid = (p.compMin + p.compMax) / 2; const compFit = mid >= m.p50 ? (mid <= m.p90 ? "aligned" : "strong") : mid >= m.p25 ? "aligned" : "gap"; const geoFit = p.country === m.country || p.geography === m.geography ? "strong" : "aligned"; return [{ dimension: "Grade level", you: p.gradeLevel, role: m.grade, market: `${m.grade} typical for this title`, fit: p.gradeLevel === m.grade ? "strong" : "aligned", negotiate: p.gradeLevel === m.grade ? "Stay at band; push equity / variable." : `Anchor as ${p.gradeLevel} doing ${m.grade} scope — ask the top of range.` }, { dimension: "Experience", you: `${p.experienceYears} years`, role: `${m.experienceMin}–${m.experienceMax} years`, market: `Median hire ~${Math.round((m.experienceMin + m.experienceMax) / 2)} yrs`, fit: expFit, negotiate: expFit === "gap" ? "Lead with outcomes, not years. Offer a 90-day proof plan." : "You sit inside the hire window — do not discount." }, { dimension: "Skills", you: p.skills.slice(0, 4).join(", ") || "—", role: job.tags.join(", "), market: `${skillHits.length}/${job.tags.length} tagged skills overlap`, fit: skillHits.length >= 2 ? "strong" : skillHits.length === 1 ? "aligned" : "gap", negotiate: skillMiss.length ? `Prep stories for: ${skillMiss.slice(0, 3).join(", ")}.` : "Skill overlap is high — trade for title or cash." }, { dimension: "Function", you: p.functionArea, role: job.function, market: job.industry, fit: p.functionArea.toLowerCase().includes(job.function.toLowerCase().split(" ")[0] ?? "") ? "strong" : "aligned", negotiate: "Map your last two cycles to this function’s KPIs in the first 10 minutes." }, { dimension: "Geography", you: p.geography, role: m.geography, market: `${m.geography} demand index high for ${job.function}`, fit: geoFit, negotiate: p.geography === m.geography ? "Local market — use local comps, not global." : "Call out mobility / timezone coverage as premium." }, { dimension: "Region", you: p.region, role: m.region, market: m.region, fit: p.region === m.region ? "strong" : "aligned", negotiate: "Region knowledge is a lever for stakeholder roles — price it." }, { dimension: "Country", you: p.country, role: m.country, market: m.country, fit: p.country === m.country ? "strong" : "gap", negotiate: p.country === m.country ? "No relocation drag — ask for the full cash band." : "Visa / relocation should sit on top of cash, not inside it." }, { dimension: "Compensation range", you: `${formatComp(p.compMin, p.currency)} – ${formatComp(p.compMax, p.currency)}`, role: `${formatComp(m.roleMin, m.currency)} – ${formatComp(m.roleMax, m.currency)}`, market: `P25 ${formatComp(m.p25, m.currency)} · P50 ${formatComp(m.p50, m.currency)} · P75 ${formatComp(m.p75, m.currency)} · P90 ${formatComp(m.p90, m.currency)}`, fit: compFit, negotiate: negotiationScript(mid, m, p.currency) }]; }
function negotiationScript(askMid: number, m: MarketBand, currency: Currency) { if (askMid < m.p50) return `Your ask sits below P50. Open at ${formatComp(m.p75, currency)} and floor at ${formatComp(m.p50, currency)}.`; if (askMid <= m.p75) return `You are in the competitive zone. Open at ${formatComp(m.p90, currency)}, walk-away ${formatComp(m.p50, currency)}.`; return `You are above P75. Justify with scope, team size, and competing offer — or trade cash for equity / sign-on.`; }
export type InterviewPack = { behavioral: string[]; role: string[]; case: string[]; compensation: string[]; reverse: string[] };
export function localInterviewPack(user: User | null, job: Job): InterviewPack { const p = profileDefaults(user); const m = JOB_MARKET[job.id]; const skill = p.skills[0] || job.tags[0] || "this domain"; return { behavioral: [`Tell me about a time you used ${skill} to move a metric that leadership actually cared about.`, `Describe a conflict with engineering or sales in the ${job.function} context. What did you change?`, `Walk through a miss. What would you do differently in the first 90 days at ${job.company}?`], role: [`How would you diagnose the ${job.tags[0] ?? job.function} problem in the first 30 days at ${job.company}?`, `What is your point of view on ${job.industry} in ${m?.country ?? p.country} over the next 18 months?`, `Which of these tags is your deepest craft — ${job.tags.join(", ")} — and where are you still learning?`], case: [`${job.company} wants +20% conversion in two quarters without adding headcount. How do you sequence the work?`, `A stakeholder wants a feature that breaks the ${job.function} roadmap. How do you decide?`, `You have messy data and a board review in 10 days. What do you ship vs. what do you stall?`], compensation: [`This role bands ${m ? `${formatComp(m.roleMin, m.currency)}–${formatComp(m.roleMax, m.currency)}` : job.salary}. Where do you sit and why?`, `If we come in at P50, which levers matter more to you: cash, equity, title, or scope?`, `What is your walk-away number, and what would make P50 acceptable?`], reverse: [`How is success measured for this ${m?.grade ?? "role"} in the first two quarters?`, `What broke the last person in this seat?`, `Where does this team lose to competitors in ${m?.region ?? p.region}?`] }; }
